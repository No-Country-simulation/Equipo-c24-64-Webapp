logging:
  level:
    org.springframework.security: DEBUG
server:
  servlet:
    context-path: /api

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    url: /api-docs
  api-docs:
    path: /api-docs

spring:

  application:
    name: "Gestion De Reservas"

  profiles:
    active: local  # o 'prod' para producción
  jpa:
    hibernate:
      ddl-auto: update